{
  "name": "pm-workflow-portable-runtime",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/runtime/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test-coverage-lines=90 --test-coverage-functions=90 --test-coverage-branches=60 --test tests/runtime/*.test.mjs",
    "check:runtime": "node scripts/sync-runtime.mjs --check"
  }
}
