# Functional wireflow contract

Use this contract after Definition approval and before Brief approval. Coverage/Journey remain product-behavior authority; Screen/State/Step only make the required functional representation inspectable.

For the final Pen manifest, use only exact artifact-grounded locators in the form `experience/<current>.pen#<real-node-id>`. The path must match the manifest's Pen source and the fragment must be a real node `id` from the current JSON tree; semantic IDs, node names, prose, and Screen-root placeholders are not Pen evidence.

## Identity and scope

- **Screen**: one stable task/context family with one primary job. URL, root-frame count, canvas position, modal/page form, and visual similarity do not define identity.
- **Material State**: a condition-dependent variant inside that Screen that materially changes visible information, available actions, feedback, or recovery while the primary job stays the same.
- **Journey Step**: one approved transition from a source State through a visible semantic trigger to immediate feedback and a destination/result, with applicable failure/recovery and re-entry.
- **Journey**: an independently enterable user goal or durable-object re-entry/recovery path. Split independent lookup, cancellation, recovery, or later retrieval responsibilities instead of merging them to make a shorter inventory; there is no fixed Journey count.
- Purpose/archetype is open, plain-language and job-shaped. Do not introduce a fixed page list, closed archetype enum, or a universal loading/empty/error/success matrix.

The prototype question, functional representation detail, interaction coverage, context/surface, and high-fidelity visual-design non-goals bound the work. Do not deliberately pursue a rough style. Tool-default neatness is acceptable, but brand, palette, type choice, shadows, decorative imagery, motion polish, and aesthetic approval are not default work.

## From Brief to artifact

1. Reconstruct approved Journey obligations from Definition locators.
2. Create the Screen inventory, Material state matrix, and Journey transition contract before asking for Brief approval.
3. For each Screen, require its primary job, meaningful content groups, functional regions/hierarchy, and primary/secondary/recovery actions.
4. For each State, require enough task context to identify the step, the real trigger/guard, visible delta, action availability, feedback/status, and recovery/next/re-entry as applicable. Compact cards are valid only when they carry those obligations; a title, legend, or status label alone is not evidence.
5. For each Step, require a visible semantic trigger/control, input/guard, immediate feedback, destination/result, and failure/recovery.
6. If decomposition needs new eligibility, input, validation, permission, lifecycle, result, commitment, notification, persistence, or recovery behavior, stop and use the Definition-return path.

Create task-shaped artifact structures in reviewable batches. A real input, choice, collection, review, or result obligation needs visible functional structure; an icon or sentence claiming the function is not a substitute. Read back descendants, content, semantic controls/signifiers, feedback, connections, recovery, re-entry, hierarchy and bounds—not only root names or bounds. Each material State must identify the state-specific descendant or variant that proves its visible delta, action availability, feedback and recovery. Several States may share one Screen root, but the shared root alone is not evidence that those States exist.

The same Screen/artboard may contain several material States as separate state-specific descendants or variants; separate pages, frames, or named roots per State are never mandatory. After the complete save/read-back pass, use `preflight-experience` before presentation with the Pen handle still live. Its canonical validator is fail-fast: repair the current target identities and rerun only after their evidence changes. Continue only when those targeted identities disappear and the next diagnostic fingerprint has not been seen; newly revealed identities are allowed. Stop when a targeted identity persists or a diagnostic fingerprint repeats.

## Functional audit

Audit inventory completeness, transition closure, feedback/recovery, and every Step's goal/action/progress/result. Then probe `template-collapse`: a shared shell or repeated layout is allowed, but different primary jobs with identical functional regions need a task reason and still must contain their own required content/control evidence.

Do not score node counts, keyword matches, product nouns, visual novelty, monochrome styling, or subtree similarity. One complete Screen may pass. Arbitrary artifact node names may pass. Justified identical state structures may pass. More frames or visual variants cannot compensate for missing content, controls, triggers, feedback, result, recovery, or re-entry.

Owner review asks whether the exact evidence expresses approved tasks, states, actions, feedback, recovery, and scope. It excludes brand/aesthetic approval and is not a real-user usability test.

Semantic review records an explicit execution/result ledger before normal preview presentation. Run one full review and, only after addressing its reported Findings, one focused recheck. A changed approved baseline, behavior scope, or artifact opens a new full review; routine consistency does not. Report over-compression with exact locators and affected obligations rather than page, node, naming, geometry, color, or aesthetic heuristics.
