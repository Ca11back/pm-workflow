interface:
  display_name: "PM Reverse Review"
  short_description: "只读复审一个不可变候选，并返回问题影响与内部整改路由"
  default_prompt: "Use $pm-reverse-review to review this existing Delivery candidate; otherwise return it to $pm-delivery first."
